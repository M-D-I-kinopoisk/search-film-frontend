import React from 'react'

type ModalFilm = {
    params: {
        id: string
    }
}

const ModalFilm = ({params: {id}}) => {
    return (
        <div>

        </div>
    )
}

export default ModalFilm